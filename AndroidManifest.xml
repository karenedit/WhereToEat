<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wheretoeat"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"></uses-permission>
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.wheretoeat.WhereEat"
             android:label="@string/app_name"
            >
             <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Login"></activity>
        <activity android:name="Registro"></activity>
        <activity android:name="Facebook"></activity>
        <activity android:name="MenuView"
            android:windowSoftInputMode = "adjustPan"></activity>        
        <activity android:name="Restaurante"></activity>
       	<activity android:name="VistaPpal"
       	    android:windowSoftInputMode = "adjustPan"
       	 ></activity>
       	<activity android:name="VistaGral"
       	    android:windowSoftInputMode = "adjustPan"
       	 ></activity>
       	
        
    </application>

</manifest>